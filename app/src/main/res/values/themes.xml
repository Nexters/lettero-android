<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Lettero" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/bg</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:textViewStyle">@style/LetteroTextViewStyle</item>
        <item name="buttonStyle">@style/LetteroButtonStyle</item>
        <item name="editTextStyle">@style/LetteroEditTextStyle</item>
    </style>

    <style name="LetteroTextViewStyle" parent="android:Widget.TextView">
        <item name="android:textColor">@color/text_black</item>
        <item name="android:fontFamily">@font/neodgm</item>
    </style>

    <style name="LetteroButtonStyle" parent="Widget.AppCompat.Button">
        <item name="android:fontFamily">@font/neodgm</item>
        <item name="android:textColor">@color/text_black</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:textAlignment">center</item>
    </style>

    <style name="LetteroEditTextStyle" parent="Widget.AppCompat.EditText">
        <item name="android:fontFamily">@font/neodgm</item>
        <item name="android:textColor">@color/text_black</item>
        <item name="colorAccent">@android:color/transparent</item>
        <item name="android:colorBackground">@android:color/transparent</item>
    </style>

    <style name="LetteroTabLayoutStyle" parent="Widget.Design.TabLayout">
        <item name="tabBackground">@drawable/selector_tab_item</item>
        <item name="tabIndicator">@android:color/transparent</item>
        <item name="tabIndicatorHeight">0dp</item>
    </style>

    <style name="LettroTabTextStyle" parent="@android:style/TextAppearance.Widget.TabWidget">
        <item name="font">@font/neodgm</item>
        <item name="android:textSize">18sp</item>
        <item name="fontStyle">normal</item>
    </style>
</resources>
